# To-Do List Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 인증 없이 할일을 등록/조회/완료토글/삭제할 수 있는 풀스택 CRUD 앱 구현

**Architecture:** Spring MVC REST API(포트 8080)가 JSON을 반환하고, 프론트엔드(Vanilla JS)가 fetch로 API를 호출한다. MyBatis가 MySQL `todo` 테이블에 SQL을 실행한다. 탭 필터(전체/미완료/완료)는 프론트에서 로컬 데이터를 필터링한다.

**Tech Stack:** Java 17, Spring MVC 5.3, MyBatis 3.5, HikariCP, MySQL 8, Jackson, HTML/CSS/Vanilla JS, Maven(Tomcat7 플러그인)

---

## 파일 구성

### 신규 생성 (back-end)
```
back-end/src/main/webapp/WEB-INF/web.xml
back-end/src/main/webapp/WEB-INF/spring/app-context.xml
back-end/src/main/webapp/WEB-INF/spring/mvc-context.xml
back-end/src/main/resources/db.properties
back-end/src/main/resources/mybatis-config.xml
back-end/src/main/resources/logback.xml
back-end/src/main/resources/mapper/TodoMapper.xml
back-end/src/main/java/com/example/vo/Todo.java
back-end/src/main/java/com/example/mapper/TodoMapper.java
back-end/src/main/java/com/example/service/TodoService.java
back-end/src/main/java/com/example/service/TodoServiceImpl.java
back-end/src/main/java/com/example/controller/TodoController.java
```

### 수정 (front-end)
```
front-end/index.html     ← 탭 UI + 입력폼 + 목록으로 교체
front-end/css/style.css  ← 탭/체크박스/삭제버튼 스타일 추가
front-end/js/api.js      ← todo CRUD 함수로 교체
front-end/js/main.js     ← 탭 필터/렌더링/이벤트 로직으로 교체
```

---

## Task 1: MySQL 테이블 생성

**Files:**
- 없음 (MySQL 클라이언트에서 직접 실행)

- [ ] **Step 1: MySQL에 DB와 테이블 생성**

MySQL 클라이언트(Workbench, DBeaver, CLI 등)에서 아래 SQL 실행:

```sql
CREATE DATABASE IF NOT EXISTS todo_db
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE todo_db;

CREATE TABLE todo (
    id         BIGINT AUTO_INCREMENT PRIMARY KEY,
    title      VARCHAR(200) NOT NULL,
    completed  TINYINT(1)   NOT NULL DEFAULT 0,
    created_at DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

- [ ] **Step 2: 테이블 확인**

```sql
DESCRIBE todo_db.todo;
```

기대 결과: id, title, completed, created_at 4개 컬럼 표시

---

## Task 2: Spring 설정 파일 작성

**Files:**
- Create: `back-end/src/main/webapp/WEB-INF/web.xml`
- Create: `back-end/src/main/webapp/WEB-INF/spring/app-context.xml`
- Create: `back-end/src/main/webapp/WEB-INF/spring/mvc-context.xml`
- Create: `back-end/src/main/resources/db.properties`
- Create: `back-end/src/main/resources/logback.xml`

- [ ] **Step 1: 디렉터리 생성**

```
back-end/src/main/webapp/WEB-INF/spring/
back-end/src/main/resources/
back-end/src/main/resources/mapper/
```

- [ ] **Step 2: web.xml 작성**

`back-end/src/main/webapp/WEB-INF/web.xml`:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
         http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

    <!-- 루트 애플리케이션 컨텍스트 (DataSource, MyBatis, Service) -->
    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>/WEB-INF/spring/app-context.xml</param-value>
    </context-param>
    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>

    <!-- UTF-8 인코딩 필터 -->
    <filter>
        <filter-name>encodingFilter</filter-name>
        <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
        <init-param><param-name>encoding</param-name><param-value>UTF-8</param-value></init-param>
        <init-param><param-name>forceEncoding</param-name><param-value>true</param-value></init-param>
    </filter>
    <filter-mapping>
        <filter-name>encodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- DispatcherServlet (Controller) -->
    <servlet>
        <servlet-name>dispatcher</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <param-value>/WEB-INF/spring/mvc-context.xml</param-value>
        </init-param>
        <load-on-startup>1</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>dispatcher</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>
</web-app>
```

- [ ] **Step 3: db.properties 작성**

`back-end/src/main/resources/db.properties` (실제 MySQL 정보로 수정 필요):

```properties
db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/todo_db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
db.username=root
db.password=your_password
```

- [ ] **Step 4: app-context.xml 작성**

`back-end/src/main/webapp/WEB-INF/spring/app-context.xml`:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/tx
           http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- db.properties 로드 -->
    <context:property-placeholder location="classpath:db.properties"/>

    <!-- HikariCP DataSource -->
    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
        <property name="driverClassName" value="${db.driver}"/>
        <property name="jdbcUrl"         value="${db.url}"/>
        <property name="username"        value="${db.username}"/>
        <property name="password"        value="${db.password}"/>
        <property name="maximumPoolSize" value="10"/>
    </bean>

    <!-- MyBatis SqlSessionFactory -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource"       ref="dataSource"/>
        <property name="configLocation"   value="classpath:mybatis-config.xml"/>
        <property name="mapperLocations"  value="classpath:mapper/*.xml"/>
    </bean>

    <!-- com.example.mapper 패키지 자동 스캔 -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="com.example.mapper"/>
    </bean>

    <!-- 트랜잭션 매니저 -->
    <bean id="transactionManager"
          class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>
    <tx:annotation-driven transaction-manager="transactionManager"/>

    <!-- Service 컴포넌트 스캔 -->
    <context:component-scan base-package="com.example.service"/>
</beans>
```

- [ ] **Step 5: mvc-context.xml 작성**

`back-end/src/main/webapp/WEB-INF/spring/mvc-context.xml`:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Controller 컴포넌트 스캔 -->
    <context:component-scan base-package="com.example.controller"/>

    <!-- Spring MVC + Jackson 자동 등록 -->
    <mvc:annotation-driven/>

    <!-- CORS: 프론트(file:// 또는 로컬 서버)에서 API 호출 허용 -->
    <mvc:cors>
        <mvc:mapping path="/api/**"
                     allowed-origins="*"
                     allowed-methods="GET,POST,PUT,DELETE,OPTIONS"
                     allowed-headers="*"/>
    </mvc:cors>
</beans>
```

- [ ] **Step 6: logback.xml 작성**

`back-end/src/main/resources/logback.xml`:

```xml
<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>
    <logger name="com.example" level="DEBUG"/>
</configuration>
```

---

## Task 3: VO + MyBatis Mapper 작성

**Files:**
- Create: `back-end/src/main/java/com/example/vo/Todo.java`
- Create: `back-end/src/main/java/com/example/mapper/TodoMapper.java`
- Create: `back-end/src/main/resources/mybatis-config.xml`
- Create: `back-end/src/main/resources/mapper/TodoMapper.xml`

- [ ] **Step 1: Todo.java 작성**

`back-end/src/main/java/com/example/vo/Todo.java`:

```java
package com.example.vo;

import java.time.LocalDateTime;

public class Todo {
    private Long id;
    private String title;
    private boolean completed;
    private LocalDateTime createdAt;

    public Long getId() { return id; }
    public void setId(Long id) { this.id = id; }

    public String getTitle() { return title; }
    public void setTitle(String title) { this.title = title; }

    public boolean isCompleted() { return completed; }
    public void setCompleted(boolean completed) { this.completed = completed; }

    public LocalDateTime getCreatedAt() { return createdAt; }
    public void setCreatedAt(LocalDateTime createdAt) { this.createdAt = createdAt; }
}
```

- [ ] **Step 2: TodoMapper.java 작성**

`back-end/src/main/java/com/example/mapper/TodoMapper.java`:

```java
package com.example.mapper;

import com.example.vo.Todo;
import java.util.List;

public interface TodoMapper {
    List<Todo> selectAll();
    void insert(Todo todo);
    void update(Todo todo);
    void delete(Long id);
}
```

- [ ] **Step 3: mybatis-config.xml 작성**

`back-end/src/main/resources/mybatis-config.xml`:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <settings>
        <!-- created_at → createdAt 자동 매핑 -->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    </settings>
    <typeAliases>
        <typeAlias type="com.example.vo.Todo" alias="Todo"/>
    </typeAliases>
</configuration>
```

- [ ] **Step 4: TodoMapper.xml 작성**

`back-end/src/main/resources/mapper/TodoMapper.xml`:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.TodoMapper">

    <select id="selectAll" resultType="Todo">
        SELECT id, title, completed, created_at
        FROM todo
        ORDER BY created_at DESC
    </select>

    <insert id="insert" parameterType="Todo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO todo (title, completed)
        VALUES (#{title}, #{completed})
    </insert>

    <update id="update" parameterType="Todo">
        UPDATE todo
        SET title = #{title}, completed = #{completed}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="Long">
        DELETE FROM todo WHERE id = #{id}
    </delete>

</mapper>
```

---

## Task 4: Service 레이어 작성

**Files:**
- Create: `back-end/src/main/java/com/example/service/TodoService.java`
- Create: `back-end/src/main/java/com/example/service/TodoServiceImpl.java`

- [ ] **Step 1: TodoService.java 작성**

`back-end/src/main/java/com/example/service/TodoService.java`:

```java
package com.example.service;

import com.example.vo.Todo;
import java.util.List;

public interface TodoService {
    List<Todo> getAll();
    Todo create(String title);
    Todo update(Long id, String title, boolean completed);
    void delete(Long id);
}
```

- [ ] **Step 2: TodoServiceImpl.java 작성**

`back-end/src/main/java/com/example/service/TodoServiceImpl.java`:

```java
package com.example.service;

import com.example.mapper.TodoMapper;
import com.example.vo.Todo;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;

@Service
@Transactional
public class TodoServiceImpl implements TodoService {

    @Autowired
    private TodoMapper todoMapper;

    @Override
    public List<Todo> getAll() {
        return todoMapper.selectAll();
    }

    @Override
    public Todo create(String title) {
        Todo todo = new Todo();
        todo.setTitle(title);
        todo.setCompleted(false);
        todoMapper.insert(todo);
        return todo;
    }

    @Override
    public Todo update(Long id, String title, boolean completed) {
        Todo todo = new Todo();
        todo.setId(id);
        todo.setTitle(title);
        todo.setCompleted(completed);
        todoMapper.update(todo);
        return todo;
    }

    @Override
    public void delete(Long id) {
        todoMapper.delete(id);
    }
}
```

---

## Task 5: Controller 작성 + 서버 기동 확인

**Files:**
- Create: `back-end/src/main/java/com/example/controller/TodoController.java`

- [ ] **Step 1: TodoController.java 작성**

`back-end/src/main/java/com/example/controller/TodoController.java`:

```java
package com.example.controller;

import com.example.service.TodoService;
import com.example.vo.Todo;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;

import java.util.List;
import java.util.Map;

@RestController
@RequestMapping("/api/todos")
public class TodoController {

    @Autowired
    private TodoService todoService;

    /** 전체 목록 조회 */
    @GetMapping
    public List<Todo> getAll() {
        return todoService.getAll();
    }

    /** 등록 - 요청: { "title": "할일 내용" } */
    @PostMapping
    @ResponseStatus(HttpStatus.CREATED)
    public Todo create(@RequestBody Map<String, String> body) {
        return todoService.create(body.get("title"));
    }

    /** 수정 - 요청: { "title": "...", "completed": true/false } */
    @PutMapping("/{id}")
    public Todo update(@PathVariable Long id,
                       @RequestBody Map<String, Object> body) {
        String title = (String) body.get("title");
        boolean completed = (Boolean) body.get("completed");
        return todoService.update(id, title, completed);
    }

    /** 삭제 */
    @DeleteMapping("/{id}")
    @ResponseStatus(HttpStatus.NO_CONTENT)
    public void delete(@PathVariable Long id) {
        todoService.delete(id);
    }
}
```

- [ ] **Step 2: db.properties에 실제 MySQL 접속 정보 입력**

`back-end/src/main/resources/db.properties`의 `db.username`, `db.password`를 실제 값으로 수정한다.

- [ ] **Step 3: 서버 기동**

`back-end` 디렉터리에서:

```bash
mvn tomcat7:run
```

기대 결과: `INFO: Starting ProtocolHandler ["http-bio-8080"]` 로그 출력, 오류 없음

- [ ] **Step 4: API 동작 확인 (curl)**

```bash
# 목록 조회 (빈 배열)
curl http://localhost:8080/api/todos

# 등록
curl -X POST http://localhost:8080/api/todos \
  -H "Content-Type: application/json" \
  -d "{\"title\":\"테스트 할일\"}"

# 목록 재조회 (등록된 항목 확인)
curl http://localhost:8080/api/todos
```

기대 결과: GET에서 `[{"id":1,"title":"테스트 할일","completed":false,...}]` 반환

- [ ] **Step 5: 커밋**

```bash
cd back-end
git add src/
git commit -m "feat: to-do list 백엔드 구현 (Spring MVC + MyBatis)"
```

---

## Task 6: 프론트엔드 api.js 교체

**Files:**
- Modify: `front-end/js/api.js`

- [ ] **Step 1: api.js 전체 교체**

`front-end/js/api.js`:

```javascript
/**
 * API 통신 모듈
 * 백엔드 REST API 호출을 담당
 */

const API_BASE_URL = 'http://localhost:8080/api';

/**
 * 공통 fetch 래퍼 - 모든 API 호출에 try-catch 적용
 */
async function fetchApi(url, options = {}) {
    try {
        const defaultOptions = {
            headers: { 'Content-Type': 'application/json' },
        };
        const mergedOptions = { ...defaultOptions, ...options };
        const response = await fetch(url, mergedOptions);

        if (!response.ok) {
            throw new Error(`HTTP 오류: ${response.status}`);
        }
        // 204 No Content (삭제 응답)는 JSON 없음
        if (response.status === 204) return null;
        return await response.json();
    } catch (error) {
        console.error('API 호출 실패:', error);
        throw error;
    }
}

/** 전체 할일 목록 조회 */
async function getTodos() {
    try {
        return await fetchApi(`${API_BASE_URL}/todos`);
    } catch (error) {
        console.error('목록 조회 실패:', error);
        throw error;
    }
}

/** 할일 등록 */
async function createTodo(title) {
    try {
        return await fetchApi(`${API_BASE_URL}/todos`, {
            method: 'POST',
            body: JSON.stringify({ title }),
        });
    } catch (error) {
        console.error('등록 실패:', error);
        throw error;
    }
}

/** 할일 수정 (제목 변경 또는 완료 토글) */
async function updateTodo(id, title, completed) {
    try {
        return await fetchApi(`${API_BASE_URL}/todos/${id}`, {
            method: 'PUT',
            body: JSON.stringify({ title, completed }),
        });
    } catch (error) {
        console.error('수정 실패:', error);
        throw error;
    }
}

/** 할일 삭제 */
async function deleteTodo(id) {
    try {
        return await fetchApi(`${API_BASE_URL}/todos/${id}`, {
            method: 'DELETE',
        });
    } catch (error) {
        console.error('삭제 실패:', error);
        throw error;
    }
}
```

---

## Task 7: 프론트엔드 UI 작성 (index.html + main.js + style.css)

**Files:**
- Modify: `front-end/index.html`
- Modify: `front-end/js/main.js`
- Modify: `front-end/css/style.css`

- [ ] **Step 1: index.html 교체**

`front-end/index.html`:

```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-Do List</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>

    <header>
        <h1>To-Do List</h1>
    </header>

    <main>
        <section id="content">
            <!-- 알림 메시지 -->
            <div id="alert" class="alert hidden"></div>

            <!-- 입력 폼 -->
            <div class="input-group">
                <input type="text" id="todoInput" placeholder="할일을 입력하세요..." maxlength="200">
                <button id="btnAdd">추가</button>
            </div>

            <!-- 탭 필터 -->
            <div class="tabs">
                <button class="tab active" data-filter="all">전체</button>
                <button class="tab" data-filter="incomplete">미완료</button>
                <button class="tab" data-filter="complete">완료</button>
            </div>

            <!-- 할일 목록 -->
            <ul id="todoList"></ul>

            <!-- 카운트 -->
            <p id="count" class="count"></p>
        </section>
    </main>

    <footer>
        <p>&copy; 2026 To-Do List</p>
    </footer>

    <script src="js/api.js"></script>
    <script src="js/main.js"></script>
</body>
</html>
```

- [ ] **Step 2: main.js 교체**

`front-end/js/main.js`:

```javascript
/**
 * UI 렌더링 및 이벤트 처리 모듈
 */

// 현재 탭 필터 상태 ('all' | 'incomplete' | 'complete')
let currentFilter = 'all';
// 전체 할일 목록 (로컬 캐시)
let todos = [];

/** 알림 메시지 표시 (3초 후 자동 숨김) */
function showAlert(message, isError = true) {
    const alertEl = document.getElementById('alert');
    alertEl.textContent = message;
    alertEl.className = `alert ${isError ? 'error' : 'success'}`;
    setTimeout(() => { alertEl.className = 'alert hidden'; }, 3000);
}

/** XSS 방지용 HTML 이스케이프 */
function escapeHtml(str) {
    return str
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#39;');
}

/** 현재 필터에 맞는 목록을 화면에 렌더링 */
function renderList() {
    const ul = document.getElementById('todoList');
    const filtered = todos.filter(todo => {
        if (currentFilter === 'incomplete') return !todo.completed;
        if (currentFilter === 'complete')   return todo.completed;
        return true;
    });

    ul.innerHTML = filtered.map(todo => `
        <li class="todo-item ${todo.completed ? 'completed' : ''}">
            <input type="checkbox" ${todo.completed ? 'checked' : ''}
                   onchange="handleToggle(${todo.id}, this.checked)">
            <span class="todo-title">${escapeHtml(todo.title)}</span>
            <button class="btn-delete" onclick="handleDelete(${todo.id})">삭제</button>
        </li>
    `).join('');

    const total = todos.length;
    const done  = todos.filter(t => t.completed).length;
    document.getElementById('count').textContent = `전체 ${total}개 · 완료 ${done}개`;
}

/** 서버에서 전체 목록 불러오기 */
async function loadTodos() {
    try {
        todos = await getTodos();
        renderList();
    } catch (error) {
        showAlert('목록을 불러오는데 실패했습니다.');
    }
}

/** 할일 추가 */
async function handleAdd() {
    const input = document.getElementById('todoInput');
    const title = input.value.trim();
    if (!title) {
        showAlert('할일을 입력해주세요.');
        return;
    }
    try {
        const newTodo = await createTodo(title);
        todos.unshift(newTodo);
        input.value = '';
        renderList();
    } catch (error) {
        showAlert('등록에 실패했습니다.');
    }
}

/** 완료 여부 토글 - title은 로컬 배열에서 조회 (HTML 속성 인젝션 방지) */
async function handleToggle(id, completed) {
    const todo = todos.find(t => t.id === id);
    if (!todo) return;
    try {
        const updated = await updateTodo(id, todo.title, completed);
        todos = todos.map(t => t.id === id ? { ...t, completed: updated.completed } : t);
        renderList();
    } catch (error) {
        showAlert('수정에 실패했습니다.');
        loadTodos(); // 실패 시 서버 상태로 복구
    }
}

/** 할일 삭제 */
async function handleDelete(id) {
    try {
        await deleteTodo(id);
        todos = todos.filter(t => t.id !== id);
        renderList();
    } catch (error) {
        showAlert('삭제에 실패했습니다.');
    }
}

/** 탭 클릭 - 필터 변경 */
function handleTabClick(filter) {
    currentFilter = filter;
    document.querySelectorAll('.tab').forEach(tab => {
        tab.classList.toggle('active', tab.dataset.filter === filter);
    });
    renderList();
}

/** 페이지 로드 시 초기화 */
document.addEventListener('DOMContentLoaded', () => {
    loadTodos();

    document.getElementById('btnAdd').addEventListener('click', handleAdd);
    document.getElementById('todoInput').addEventListener('keydown', e => {
        if (e.key === 'Enter') handleAdd();
    });
    document.querySelectorAll('.tab').forEach(tab => {
        tab.addEventListener('click', () => handleTabClick(tab.dataset.filter));
    });
});
```

- [ ] **Step 3: style.css 교체**

`front-end/css/style.css`:

```css
* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

body {
    font-family: 'Segoe UI', sans-serif;
    background: #f0f2f5;
    color: #333;
}

header {
    background: #4f46e5;
    color: #fff;
    padding: 20px;
    text-align: center;
}

header h1 { font-size: 1.8rem; }

main {
    max-width: 600px;
    margin: 30px auto;
    padding: 0 16px;
}

footer {
    text-align: center;
    padding: 20px;
    color: #888;
    font-size: 0.85rem;
}

/* 알림 메시지 */
.alert {
    padding: 10px 14px;
    border-radius: 6px;
    margin-bottom: 12px;
    font-size: 0.9rem;
}
.alert.hidden  { display: none; }
.alert.error   { background: #fee2e2; color: #b91c1c; }
.alert.success { background: #dcfce7; color: #15803d; }

/* 입력 폼 */
.input-group {
    display: flex;
    gap: 8px;
    margin-bottom: 16px;
}

.input-group input {
    flex: 1;
    padding: 10px 14px;
    border: 1px solid #d1d5db;
    border-radius: 6px;
    font-size: 1rem;
    outline: none;
}
.input-group input:focus { border-color: #4f46e5; }

.input-group button {
    padding: 10px 20px;
    background: #4f46e5;
    color: #fff;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    font-size: 1rem;
}
.input-group button:hover { background: #4338ca; }

/* 탭 필터 */
.tabs {
    display: flex;
    gap: 8px;
    margin-bottom: 16px;
}

.tab {
    padding: 7px 16px;
    border: 1px solid #d1d5db;
    border-radius: 20px;
    background: #fff;
    cursor: pointer;
    font-size: 0.9rem;
    color: #555;
}
.tab.active { background: #4f46e5; color: #fff; border-color: #4f46e5; }
.tab:hover:not(.active) { background: #f3f4f6; }

/* 할일 목록 */
#todoList {
    list-style: none;
    background: #fff;
    border-radius: 8px;
    overflow: hidden;
    border: 1px solid #e5e7eb;
}

.todo-item {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 12px 16px;
    border-bottom: 1px solid #e5e7eb;
}
.todo-item:last-child { border-bottom: none; }

.todo-item.completed .todo-title {
    text-decoration: line-through;
    color: #9ca3af;
}

.todo-item input[type="checkbox"] {
    width: 18px;
    height: 18px;
    cursor: pointer;
    accent-color: #4f46e5;
}

.todo-title { flex: 1; font-size: 0.95rem; }

.btn-delete {
    padding: 4px 10px;
    background: #fff;
    border: 1px solid #e5e7eb;
    border-radius: 4px;
    cursor: pointer;
    color: #ef4444;
    font-size: 0.8rem;
}
.btn-delete:hover { background: #fee2e2; }

/* 카운트 */
.count {
    margin-top: 10px;
    font-size: 0.85rem;
    color: #888;
    text-align: right;
}
```

- [ ] **Step 4: 브라우저에서 동작 확인**

1. 백엔드 서버가 `http://localhost:8080`에서 실행 중인지 확인
2. `front-end/index.html`을 브라우저에서 열기 (file://)
3. 아래 시나리오 검증:
   - 할일 입력 후 추가 → 목록에 표시
   - 체크박스 클릭 → 취소선 표시, 완료 탭에서 확인
   - 미완료 탭 클릭 → 완료 항목 숨김
   - 삭제 버튼 클릭 → 목록에서 제거
   - 빈 값 추가 시도 → 알림 메시지 표시

- [ ] **Step 5: 커밋**

```bash
cd front-end
git add index.html css/style.css js/api.js js/main.js
git commit -m "feat: to-do list 프론트엔드 구현 (탭 필터 + CRUD)"
```
